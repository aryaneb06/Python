s={ 1,3, 4} # it contain values only which does not allow repeatation (just unique) and set is unordered whether dict is ordered
print(type(s))

e=set() #empty set   DOnt use s={} as it will create a empty dictionary thats is generally asked in interview

f={ 2,3, 5, 3, 36, 56, 3 ,"Aryan"} # it shows 3 only once in result and order can be unordered 
print(f,type(f))


# sets are unordered, unindexed , we cant change the items ,cannot contain duplicate values